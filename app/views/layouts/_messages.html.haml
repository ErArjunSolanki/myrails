- flash.each do |name, msg|
  - if msg.is_a?(String)
    %div{id: "flash_#{name}", class: "alert alert-dismissible alert-#{name}"}
      %button{class: :close, "data-dismiss" => :alert, "aria-hidden" => true}
        &times;
      =msg