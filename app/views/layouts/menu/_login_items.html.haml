.container
  %ul.pull-right
    - if user_signed_in? 
      %li= link_to "My Account (#{current_user.email})"        
      %li= link_to 'Logout', destroy_user_session_path, :method => :delete
    - else      
      %li= link_to('New User', new_user_session_path)
            
