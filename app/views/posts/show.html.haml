.row
  =render 'left_bar'
  .col-md-9
    %article
      %h1= @post.title
      .post-info
        Published at
        %span=l @post.created_at, format: "%B %d, %Y"
        by
        %span Blogger
      #content=raw @post.content
      -# =render 'comments'
- if user_signed_in?
  = link_to 'Editer', edit_post_path(@post)
  \|
  = link_to 'Return to list', posts_path