# (1..20).each do |index|
	# Post.create(:title => "post #{index}" , :content => "post content #{index}" , :meta_title => "meta title #{index}", :meta_desc => "meta desc #{index}", :slug => "post-#{index}", :publish => 1)
# end
